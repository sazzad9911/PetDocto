import React from 'react';
import {Scrollbar,Text, ScrollView} from 'react-native'
const Calender = () => {
    return (
        <ScrollView>
            <Text>Calender</Text>
        </ScrollView>
    );
};

export default Calender;