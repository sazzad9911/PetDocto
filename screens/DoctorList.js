import React from 'react';
import {Scrollbar,ScrollView,Text} from 'react-native';

const DoctorList = () => {
    return (
        <ScrollView>
            <Text>akash</Text>
        </ScrollView>
    );
};

export default DoctorList;