import React from 'react';
import {Scrollbar} from 'react-native';

const DoctorList = () => {
    return (
        <ScrollView>
            
        </ScrollView>
    );
};

export default DoctorList;