import React from 'react';
import { View, Text, SafeAreaView, ScrollView} from 'react-native';

const Home = () => {
    return (
        <ScrollView>
            <View>
                <Text>hghg</Text>
            </View>
        </ScrollView>
    );
};

export default Home;