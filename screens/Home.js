import React from 'react';
import {View,Text,ScrollView} from 'react-native';

const Home = () => {
    return (
        <ScrollView>
            
        </ScrollView>
    );
};

export default Home;