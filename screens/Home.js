import React from 'react';

import { View, Text, SafeAreaView, ScrollView} from 'react-native';

const Home = () => {
    return (
        <ScrollView>

        </ScrollView>
    );
};

export default Home;